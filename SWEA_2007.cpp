#include <iostream>
#include <string>

using namespace std;

int main(){
    int T;

    cin >> T;

    for(int i = 0;i < T;i++){
        string pattern;
        cin >> pattern;
        for(int j = 0;j < pattern.length()/2;j++){
            string temp;
            if(j == 0)
                temp[j] = pattern[j];
            
        }
    }
}